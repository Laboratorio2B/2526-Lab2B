

## Esercizi



### Uso di semafori con incrementi generici (17/3/26)

Riscrivere il programma `heap.c` in modo che gestisca l'heap utilizzando le funzioni `zem_up` e `zem_down` 
invece delle funzioni `libera` e `richiedi`.


